projection implementation in class ZBP_C_DAKM_LANG unique;
strict ( 2 );
use draft;
define behavior for ZC_DAKM_LANG alias ZcDakmLang
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}